<section class="slide-summary">
  <h2>Review Complete</h2>
  <p class="summary-text">{{slide.summary}}</p>

  <div class="stats-row">
    <div class="stat">
      <span class="stat-num">{{slide.totalFiles}}</span>
      <span class="stat-label">files changed</span>
    </div>
    <div class="stat stat-add">
      <span class="stat-num">+{{slide.additions}}</span>
      <span class="stat-label">additions</span>
    </div>
    <div class="stat stat-del">
      <span class="stat-num">-{{slide.deletions}}</span>
      <span class="stat-label">deletions</span>
    </div>
  </div>

  {{#if slide.risks.length}}
  <div class="final-risks">
    <h4>Key risks to keep in mind</h4>
    <ul>{{#each slide.risks}}<li>{{this}}</li>{{/each}}</ul>
  </div>
  {{/if}}
</section>
