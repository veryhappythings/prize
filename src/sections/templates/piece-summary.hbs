<div class="section-piece-summary">
  <div class="piece-counter">{{section.pieceIndex}} / {{section.totalPieces}}</div>
  <h2>{{section.name}}</h2>
  <div class="piece-description">{{{md section.description}}}</div>
  <ul class="file-list">
    {{#each section.files}}<li>{{{file_link this}}}</li>{{/each}}
  </ul>
</div>
