<div class="section-issues">
  <h3>{{section.pieceName}} — Issues</h3>
  <ul class="issues-list">
    {{#each section.issues}}
    <li class="issue issue-{{severity}}">
      <span class="severity-badge">{{severity}}</span>
      <div class="issue-body">{{{md description}}}</div>
    </li>
    {{/each}}
  </ul>
</div>
